const userData = require('./users');
const reviewData = require('./reviews');
const beerData = require('./beers')

module.exports = { 
    users: userData,
    reviews: reviewData,
    beers: beerData
};